<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="bootstrap.min.css" />
    <title>Editar valor</title>
  </head>
  <body>
    <div>Aquí la edición</div>

    <% let des, i; %> 
    <% for (i=0; i < listadoEventos.length; i++) {%> 
        <% if(listadoEventos[i].id == elemento) { %>
            <% des = listadoEventos[i].descripcion %>
            <% ind = listadoEventos[i].id %>          
        <% }} %>

    <form action="/editarD/<%= ind %> " method="POST">
      <label for="edit">Cambia el Evento</label>
      <input
        type="text"
        id="edit"
        name="edit"
        value="<%= des %>"
      />
      <input type="submit" value="Entrar" />
    </form>
  </body>
</html>
